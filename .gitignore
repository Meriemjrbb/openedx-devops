# ── Secrets / environment ────────────────────────────────
.env
.env.*
*.pem
*.key
id_rsa*
id_ed25519*
.vault_pass
vault-password*

# ── Ansible ──────────────────────────────────────────────
*.retry
ansible/collections/
ansible/.cache/

# ── Vagrant ──────────────────────────────────────────────
.vagrant/
*.box

# ── Terraform ────────────────────────────────────────────
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
.terraform/
.terraform.lock.hcl
terraform/crash.log

# ── Editors / OS ─────────────────────────────────────────
.idea/
.vscode/
*.swp
.DS_Store

# ── Local session notes (not project deliverables) ───────
.sessions/
session-*.md

# ── Personal command pipelines (local reference only) ────
.commands/
.ansible/

# Personal proof-of-work doc (not a deliverable)
docs/dod.md
docs/jdex.md
